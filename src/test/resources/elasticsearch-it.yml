cluster:
    name: elasticsearch
    info:
        update:
            interval: 5m

node:
    name: "Thomas Young"

index :
    store:
        type: memory

http.cors.enabled: true

cluster:
    routing:
        allocation:
            disk:
                watermark:
                    low: 500mb
